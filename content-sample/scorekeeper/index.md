
# Scorekeeper

Track scores for any game in real time. Docs live on the same drive in the
`scorekeeper/` folder, so updating them is as easy as editing a Markdown file.

## Quick start

```bash
pnpm install
vercel --prod --yes
```
